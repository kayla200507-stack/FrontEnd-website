import React, { useState } from 'react';
import { 
  User, Lock, Bell, Camera, Save, 
  LayoutDashboard, Search, FileText, ClipboardList, 
  Calendar, Settings, LogOut, ChevronRight
} from 'lucide-react';

const SettingsPage = () => {
  const [activeTab, setActiveTab] = useState('profile');

  return (
    <div className="flex min-h-screen bg-[#F8FAFC] font-sans text-slate-900">
      {/* SIDEBAR */}
      <aside className="w-64 bg-white border-r border-slate-200 flex flex-col">
        <div className="p-6">
          <div className="flex items-center gap-2 text-blue-600 mb-8">
            <div className="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center">
              <FileText className="text-white w-5 h-5" />
            </div>
            <span className="text-xl font-bold tracking-tight text-slate-800">Vokasi Magang</span>
          </div>

          <nav className="space-y-1">
            <NavItem icon={<LayoutDashboard size={20} />} label="Dashboard" />
            <NavItem icon={<Search size={20} />} label="Lowongan" />
            <NavItem icon={<ChevronRight size={20} />} label="Status" />
            <NavItem icon={<ClipboardList size={20} />} label="Logbook" />
            <NavItem icon={<FileText size={20} />} label="Laporan" />
            <NavItem icon={<Calendar size={20} />} label="Kalender" />
          </nav>
        </div>

        <div className="mt-auto p-6 space-y-1 border-t border-slate-100">
          <NavItem icon={<Settings size={20} />} label="Settings" active />
          <NavItem icon={<LogOut size={20} />} label="Log out" />
        </div>
      </aside>

      {/* MAIN CONTENT */}
      <main className="flex-1 flex flex-col">
        {/* HEADER */}
        <header className="h-20 bg-white border-b border-slate-200 px-8 flex items-center justify-between">
          <h1 className="text-2xl font-bold text-slate-800">Settings</h1>
          <div className="flex items-center gap-3">
            <div className="text-right hidden sm:block">
              <p className="text-sm font-bold text-slate-800">Zaidan Fahry</p>
              <p className="text-xs text-slate-500">D3 Teknologi Informasi</p>
            </div>
            <img 
              src="https://ui-avatars.com/api/?name=Zaidan+Fahry&background=3b82f6&color=fff" 
              alt="Profile" 
              className="w-10 h-10 rounded-full border-2 border-blue-50"
            />
          </div>
        </header>

        {/* CONTENT AREA */}
        <div className="p-8 max-w-4xl">
          <div className="mb-8">
            <h2 className="text-xl font-bold text-slate-800">Pengaturan Akun</h2>
            <p className="text-slate-500 text-sm">Kelola informasi profil dan keamanan akun Anda</p>
          </div>

          <div className="flex gap-8">
            {/* TABS NAVIGATION */}
            <div className="w-48 flex flex-col gap-2">
              <TabButton 
                active={activeTab === 'profile'} 
                onClick={() => setActiveTab('profile')}
                icon={<User size={18} />}
                label="Profil"
              />
              <TabButton 
                active={activeTab === 'security'} 
                onClick={() => setActiveTab('security')}
                icon={<Lock size={18} />}
                label="Keamanan"
              />
              <TabButton 
                active={activeTab === 'notifications'} 
                onClick={() => setActiveTab('notifications')}
                icon={<Bell size={18} />}
                label="Notifikasi"
              />
            </div>

            {/* FORM SECTION */}
            <div className="flex-1 bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
              {activeTab === 'profile' && <ProfileForm />}
              {activeTab === 'security' && <SecurityForm />}
              {activeTab === 'notifications' && <NotificationSettings />}
            </div>
          </div>
        </div>
      </main>
    </div>
  );
};

// Sub-komponen pendukung
const NavItem = ({ icon, label, active = false }) => (
  <div className={`flex items-center gap-3 px-4 py-3 rounded-xl cursor-pointer transition-all ${
    active ? 'bg-blue-50 text-blue-600 font-bold' : 'text-slate-500 hover:bg-slate-50'
  }`}>
    {icon}
    <span className="text-sm">{label}</span>
  </div>
);

const TabButton = ({ active, onClick, icon, label }) => (
  <button 
    onClick={onClick}
    className={`flex items-center gap-3 px-4 py-3 rounded-xl transition-all text-sm font-medium ${
      active ? 'bg-slate-900 text-white shadow-lg shadow-slate-200' : 'text-slate-500 hover:bg-white hover:shadow-sm'
    }`}
  >
    {icon}
    {label}
  </button>
);

const ProfileForm = () => (
  <div className="p-8">
    <div className="flex items-center gap-6 mb-8">
      <div className="relative">
        <img 
          src="https://ui-avatars.com/api/?name=Zaidan+Fahry&background=3b82f6&color=fff&size=128" 
          alt="Avatar Large" 
          className="w-24 h-24 rounded-2xl object-cover border-4 border-slate-50"
        />
        <button className="absolute -bottom-2 -right-2 p-2 bg-blue-600 text-white rounded-lg shadow-lg hover:bg-blue-700 transition">
          <Camera size={16} />
        </button>
      </div>
      <div>
        <h3 className="font-bold text-slate-800">Foto Profil</h3>
        <p className="text-xs text-slate-500 mt-1">Format JPG atau PNG, maksimal 2MB.</p>
      </div>
    </div>

    <div className="grid grid-cols-2 gap-6">
      <InputGroup label="Nama Lengkap" value="Zaidan Fahry" />
      <InputGroup label="NIM" value="2141720000" />
      <InputGroup label="Email" value="zaidan.fahry@student.mail.com" />
      <InputGroup label="No. Telepon" value="+62 812 3456 7890" />
      <div className="col-span-2">
        <InputGroup label="Program Studi" value="D3 Teknologi Informasi" disabled />
      </div>
    </div>

    <div className="mt-8 pt-6 border-t border-slate-100 flex justify-end">
      <button className="flex items-center gap-2 bg-blue-600 text-white px-6 py-2.5 rounded-xl font-bold hover:bg-blue-700 transition shadow-lg shadow-blue-100">
        <Save size={18} /> Simpan Perubahan
      </button>
    </div>
  </div>
);

const SecurityForm = () => (
  <div className="p-8">
    <h3 className="font-bold text-slate-800 mb-6">Ubah Kata Sandi</h3>
    <div className="space-y-4 max-w-md">
      <InputGroup label="Kata Sandi Sekarang" type="password" />
      <InputGroup label="Kata Sandi Baru" type="password" />
      <InputGroup label="Konfirmasi Kata Sandi Baru" type="password" />
    </div>
    <div className="mt-8 pt-6 border-t border-slate-100 flex justify-end">
      <button className="bg-slate-900 text-white px-6 py-2.5 rounded-xl font-bold hover:bg-slate-800 transition">
        Update Password
      </button>
    </div>
  </div>
);

const NotificationSettings = () => (
  <div className="p-8">
    <h3 className="font-bold text-slate-800 mb-6">Preferensi Notifikasi</h3>
    <div className="space-y-6">
      <ToggleItem title="Email Notifikasi" desc="Terima update status magang melalui email" checked />
      <ToggleItem title="Pengingat Logbook" desc="Notifikasi harian untuk mengisi logbook" checked />
      <ToggleItem title="Pesan Dosen" desc="Beritahu jika dosen pembimbing mengirim pesan" />
    </div>
  </div>
);

const InputGroup = ({ label, value, type = "text", disabled = false }) => (
  <div className="flex flex-col gap-1.5">
    <label className="text-xs font-bold text-slate-500 uppercase tracking-wider">{label}</label>
    <input 
      type={type}
      defaultValue={value}
      disabled={disabled}
      className="px-4 py-2.5 rounded-xl border border-slate-200 bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-500/20 focus:border-blue-500 transition outline-none text-sm disabled:opacity-60"
    />
  </div>
);

const ToggleItem = ({ title, desc, checked = false }) => (
  <div className="flex items-center justify-between p-4 rounded-2xl bg-slate-50 border border-slate-100">
    <div>
      <p className="text-sm font-bold text-slate-800">{title}</p>
      <p className="text-xs text-slate-500">{desc}</p>
    </div>
    <div className={`w-12 h-6 rounded-full p-1 transition-all cursor-pointer ${checked ? 'bg-blue-600' : 'bg-slate-300'}`}>
      <div className={`w-4 h-4 bg-white rounded-full shadow-sm transition-all ${checked ? 'translate-x-6' : 'translate-x-0'}`} />
    </div>
  </div>
);

export default SettingsPage;